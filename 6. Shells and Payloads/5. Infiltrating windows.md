
## Enumerating Windows & Fingerprinting Methods

>How do we identify if a host is windows or linux?????
>Answer : when using ICMP (Ping request) to check if a host is up, if it's windows, typically the TTL value will be 32 or 128.

- We can use multiple types paylods for windows hosts.. DLLs, batch files, msi packages and even powershell scripts
- All of these file types are executable in windows.

#### DLL
- Dynamic Link Library
- library file used in Microsoft operating systems to provide shared code and data that can be used by many different programs at once.
#### Batch
- DOS scripts utilized by the system admins to complete multiple tasks through the command-line interpreter.
- .bat extension
#### VBS
- light weight scripting language based on microsoft's visual basic.
#### MSI

- serve as installation database for windows installer.
- once we have it in the host, we can run `msiexec` to execute the file.

#### Powershell
- both a shell environment and scripting language.

## Tools, Tactics, and Procedures for Payload Generation, Transfer, and Execution

