#### Logging in to mysql

```python
mysql -u root -p
```
##### Specifying a remote host
```python
mysql -u root -h docker.hackthebox.eu -P 3306 -p
```

- A list of tables in the current database can be obtained using the **SHOW TABLES**, **DESCRIBE** keyword is used to list the table structure with  its fields and data types.
