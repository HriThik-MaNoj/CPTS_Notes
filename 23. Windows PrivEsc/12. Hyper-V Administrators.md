> members of this group has full access to all `Hyper-V` features
> If Domain controllers virtualized -> Virtualization admins are considered Domain Admins.

- Create a clone of the live Domain controller
- Mount the virtual disk offline to obtain the `NTDS.dit` file
- Extract `NTLM` passwd hashes for all users in the domain.

### Hyper-V VMMS Hard Link Abuse

- Delete `.vhdx` → `vmms.exe` restores permissions as `SYSTEM` without impersonation.
- Replace `.vhdx` with a **native hard link** → point it to a protected SYSTEM file.
- Exploit via [**CVE-2018-0952](https://www.tenable.com/cve/CVE-2018-0952) / [CVE-2019-0841**](https://www.tenable.com/cve/CVE-2019-0841).
- Alternative: abuse a **SYSTEM service** that unprivileged users can start.

## Target File
```python
### Hyper-V Hard Link → SYSTEM via Mozilla Maintenance Service

**Target:** `C:\Program Files (x86)\Mozilla Maintenance Service\maintenanceservice.exe`

1. **Grant full permissions**
   - Modify/run the Hyper-V hard-link PoC so the current user gets full control of `maintenanceservice.exe`.

2. **Take ownership**
   ```cmd
   takeown /F "C:\Program Files (x86)\Mozilla Maintenance Service\maintenanceservice.exe"
```

→ Take ownership after gaining file control.

3. **Replace & execute**
    
    - Replace `maintenanceservice.exe` with a malicious executable.
    
    ```
    sc.exe start MozillaMaintenance
    ```
    
    → Start the Mozilla Maintenance Service → executes the replaced binary as **SYSTEM**.
    

> **Mitigation:** March 2020 Windows security updates changed hard-link behavior and mitigated this vector.