- Changing the HTTP methods within the request to produce unintended results.
- For example, if a certain web application is not developed to handle other types of HTTP requests like HEAD or PUT, then we may be able to exploit it and gain access to unintended functionalities.

| Verb | Description |
| :--- | :--- |
| **HEAD** | Identical to a GET request, but its response only contains the headers, without the response body |
| **PUT** | Writes the request payload to the specified location |
| **DELETE** | Deletes the resource at the specified location |
| **OPTIONS** | Shows different options accepted by a web server, like accepted HTTP verbs |
| **PATCH** | Apply partial modifications to the resource at the specified location |

## Basic Authentication Bypass
- When we get a prompt to enter credentials to access certain parts of the web page, we can try tampering the HTTP request method to gain access.

**Checking the types of requests accepted by a server**
```python
curl -i -X OPTIONS http://SERVER_IP:PORT/
```

![[Pasted image 20260623130701.png]]


