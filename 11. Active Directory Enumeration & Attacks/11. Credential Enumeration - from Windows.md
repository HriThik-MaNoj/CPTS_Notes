## ActiveDirectory PowerShell Module

- Group of cmdlets for administering an AD environment from the command line.
#### Discover Modules
```python
PS C:\htb> Get-Module
```

#### Load ActiveDirectory Module

```python
PS C:\htb> Import-Module ActiveDirectory PS C:\htb> Get-Module
```


**Now our that our modules are loaded, lets begin..**

#### Get Domain Info

```python
PS C:\htb> Get-ADDomain
# Prints out information like domain SID, domain functional level, any child domains ect.
```

#### Get-ADUser

```python
PS C:\htb> Get-ADUser -Filter {ServicePrincipalName -ne "$null"} -Properties ServicePrincipalName

# Filters for accounts with ServicePrincipal Name property populated. These accounts may be susceptible to Kerberoasting attack.
```

#### Checking For Trust Relationships

```python
PS C:\htb> Get-ADTrust -Filter *

# This cmdlet will print out any trust relationships the domain has. We can determine if they are trusts within our forest or with domains in other forests, the type of trust, the direction of the trust, and the name of the domain the relationship is with. This will be useful later on when looking to take advantage of child-to-parent trust relationships and attacking across forest trusts.
```


#### Group Enumeration

```python
PS C:\htb> Get-ADGroup -Filter * | select name
```
- We can take the results and feed interesting names back into the cmdlet to get more detailed info about a particular group like so:
#### Detailed Group Info
```python
PS C:\htb> Get-ADGroup -Identity "Backup Operators"
```

### Group Membership

```python
PS C:\htb> Get-ADGroupMember -Identity "Backup Operators"
```
![[Pasted image 20260404100333.png]]

- We can see that one account, `backupagent` belongs to this group.
- Similarly we can repeat the same process with different group.
- This info can be particularly useful if we can somehow compromise the accounts within these groups which can lead to domain take over.
- We can do this in a `faster way` using `bloodhound` but this way is more stealthier since we aren't dropping anything in the victim machine.

## PowerView
- Helps gain situational awareness in an AD environment.
- Much like Bloodhound, it provides a way t identify where users are logged in on a network.
- Enumerate domain info such as users, computers , groups, ACLS, truss, hunt for file shares and passwords, perform Kerberoasting and more.

| **Command**                         | **Description**                                                                            |
| ----------------------------------- | ------------------------------------------------------------------------------------------ |
| `Export-PowerViewCSV`               | Append results to a CSV file                                                               |
| `ConvertTo-SID`                     | Convert a User or group name to its SID value                                              |
| `Get-DomainSPNTicket`               | Requests the Kerberos ticket for a specified Service Principal Name (SPN) account          |
| **Domain/LDAP Functions:**          |                                                                                            |
| `Get-Domain`                        | Will return the AD object for the current (or specified) domain                            |
| `Get-DomainController`              | Return a list of the Domain Controllers for the specified domain                           |
| `Get-DomainUser`                    | Will return all users or specific user objects in AD                                       |
| `Get-DomainComputer`                | Will return all computers or specific computer objects in AD                               |
| `Get-DomainGroup`                   | Will return all groups or specific group objects in AD                                     |
| `Get-DomainOU`                      | Search for all or specific OU objects in AD                                                |
| `Find-InterestingDomainAcl`         | Finds object ACLs in the domain with modification rights set to non-built in objects       |
| `Get-DomainGroupMember`             | Will return the members of a specific domain group                                         |
| `Get-DomainFileServer`              | Returns a list of servers likely functioning as file servers                               |
| `Get-DomainDFSShare`                | Returns a list of all distributed file systems for the current (or specified) domain       |
| **GPO Functions:**                  |                                                                                            |
| `Get-DomainGPO`                     | Will return all GPOs or specific GPO objects in AD                                         |
| `Get-DomainPolicy`                  | Returns the default domain policy or the domain controller policy for the current domain   |
| **Computer Enumeration Functions:** |                                                                                            |
| `Get-NetLocalGroup`                 | Enumerates local groups on the local or a remote machine                                   |
| `Get-NetLocalGroupMember`           | Enumerates members of a specific local group                                               |
| `Get-NetShare`                      | Returns open shares on the local (or a remote) machine                                     |
| `Get-NetSession`                    | Will return session information for the local (or a remote) machine                        |
| `Test-AdminAccess`                  | Tests if the current user has administrative access to the local (or a remote) machine     |
| **Threaded 'Meta'-Functions:**      |                                                                                            |
| `Find-DomainUserLocation`           | Finds machines where specific users are logged in                                          |
| `Find-DomainShare`                  | Finds reachable shares on domain machines                                                  |
| `Find-InterestingDomainShareFile`   | Searches for files matching specific criteria on readable shares in the domain             |
| `Find-LocalAdminAccess`             | Find machines on the local domain where the current user has local administrator access    |
| **Domain Trust Functions:**         |                                                                                            |
| `Get-DomainTrust`                   | Returns domain trusts for the current domain or a specified domain                         |
| `Get-ForestTrust`                   | Returns all forest trusts for the current forest or a specified forest                     |
| `Get-DomainForeignUser`             | Enumerates users who are in groups outside of the user's domain                            |
| `Get-DomainForeignGroupMember`      | Enumerates groups with users outside of the group's domain and returns each foreign member |
| `Get-DomainTrustMapping`            | Will enumerate all trusts for the current domain and any others seen.                      |

#### Domain User Information
- This can provide us with information on all users or specific users we specify

```python
PS C:\htb> Get-DomainUser -Identity mmorgan -Domain inlanefreight.local | Select-Object -Property name,samaccountname,description,memberof,whencreated,pwdlastset,lastlogontimestamp,accountexpires,admincount,userprincipalname,serviceprincipalname,useraccountcontrol
```
![[Pasted image 20260404101655.png]]

Now let's enumerate some domain group information. We can use the [Get-DomainGroupMember](https://powersploit.readthedocs.io/en/latest/Recon/Get-DomainGroupMember/) function to retrieve group-specific information. Adding the `-Recurse` switch tells PowerView that if it finds any groups that are part of the target group (nested group membership) to list out the members of those groups.

#### Recursive Group Membership
```python
PS C:\htb> Get-DomainGroupMember -Identity "Domain Admins" -Recurse

# Performs a recursive look at the Domain admins group and list its memebers.
```

![[Pasted image 20260404102319.png]]

Now we know whom to target for potential elevation of privileges, we can also enumerate domain trust mappings

#### Trust Enumeration

```python
PS C:\htb> Get-DomainTrustMapping
```

#### Testing for Local Admin Access
- we can use Test-AdminAccesss function to test for local admin access on either the current machine or a remote one.
```python
PS C:\htb> Test-AdminAccess -ComputerName ACADEMY-EA-MS01
```

![[Pasted image 20260404103258.png]]

#### Finding Users With SPN Set
- These users maybe susceptible to Kerberoasting attack.

```python
PS C:\htb> Get-DomainUser -SPN -Properties samaccountname,ServicePrincipalName
```
![[Pasted image 20260404103429.png]]

## SharpView
- .NET port of Powerview
##### Using Sharpview to get information about a particular user

```python
.\SharpView.exe Get-DomainUser -Identity forend
```

![[Pasted image 20260404105940.png]]

## Snaffler
- Tool that helps us acquire credentials or other sensitive data in an AD environment.
#### Snaffler Execution

```python
Snaffler.exe -s -d inlanefreight.local -o snaffler.log -v data
```
- -s : prints results to the console
-  - d: specifies the domain to search within
- -o : Write results to a log file
- -v verbosity level (data is best typically)

## Bloodhound

#### Sharphound
- Collecting data to feed to bloodhound GUI
```python
PS C:\htb> .\SharpHound.exe -c All --zipfilename ILFREIGHT
```
- Exfiltrate this data back to our attack host. Upload it to bloodhound GUI.
- We can start by typing `domain:` in the search bar on the top left and choosing `INLANEFREIGHT.LOCAL` from the results.
- 