```python
rustscan -a 10.129.58.16 --ulimit 5000 -- -A -T4 -oA nmap_scan
```

- Just two ports open, ssh and http:
Open 10.129.58.16:22
Open 10.129.58.16:80

- lets checkout the web server
- nothing super interesting on the web server, viewed the source as well.. nothing!

### directory bruteforcing with gobuster
```python
gobuster dir -u http://10.129.58.16// -w /usr/share/seclists/Discovery/Web-Content/DirBuster-2007_directory-list-2.3-medium.txt -t 15 -o gobuster_postman.txt
```
- found an `upload` and `images` directories, but nothing interesting there :)

- upon doing a full port scan on the system again we found out that `redis` was running on port `6379`

