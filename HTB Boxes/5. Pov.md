# Enumeration

## Nmap
```python
rustscan -a 10.129.58.115 --ulimit 5000 -- -A -T4 -oA nmap_scan
```

- Only port 80 is open and tried subdomain bruteforcing on that domain.. nothing!

```python
gobuster dir -u http://pov.htb/ -w /usr/share/seclists/Discovery/Web-Content/DirBuster-2007_directory-list-2.3-medium.txt -t 15 -o gobuster_pov.txt
```

**Trying vhost enumeration next**
```python
gobuster vhost -u http://pov.htb -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-110000.txt -t 60 --append-domain
```

- found a new subdomain : `dev.pov.htb`
- didn't get much out of directory bruteforcing

- Going through the website, we spot a download cv button which downloads the cv of the developer
- triying lfi on the `file parameter` within that request 
![[Pasted image 20260906145800.png]]

- got `web.config`
- lets examine that next

