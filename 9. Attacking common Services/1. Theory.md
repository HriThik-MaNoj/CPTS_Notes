## SMB
- we can interact with SMB in windows in multiple ways.
- [winkey] + [R] to open dialogue box and type the share location eg: `\\192.168.220.129\Finance\`
- If anyonymous login in enabled, we wont get password prompt, else we will..
![[Pasted image 20260307104446.png|526]]

#### Windows CMD - DIR

```
dir \\192.168.220.129\Finance\
```

#### Windows CMD - Net Use

```
net use n: \\192.168.220.129\Finance
```

```
net use n: \\192.168.220.129\Finance /user:plaintext Password123
```

#### Command to determine how many files exist in that share

```
dir n: /a-d /s /b | find /c ":\"
```

#### Windows CMD - Findstr

```
findstr /s /i cred n:\*.*
```


#### Windows PowerShell
- we can run windows comments in both cmd and powershell. but we cannot run all cmdlets (powershell commands) in cmd.
```
Get-ChildItem \\192.168.220.129\Finance\
```

```
New-PSDrive -Name "N" -Root
```

#### Windows PowerShell - PSCredential Object

```
PS C:\htb> $username = 'plaintext' PS C:\htb> $password = 'Password123' PS C:\htb> $secpassword = ConvertTo-SecureString $password -AsPlainText -Force PS C:\htb> $cred = New-Object System.Management.Automation.PSCredential $username, $secpassword PS C:\htb> New-PSDrive -Name "N" -Root "\\192.168.220.129\Finance" -PSProvider "FileSystem" -Credential $cred Name Used (GB) Free (GB) Provider Root CurrentLocation ---- --------- --------- -------- ---- --------------- N FileSystem \\192.168.220.129\Finance
```

#### Windows PowerShell - GCI
- We can use the property `-Include` to find specific items from the directory specified by the Path parameter.

```
Get-ChildItem -Recurse -Path N:\ -Include *cred* -File
```

#### Windows PowerShell - Select-String (Uses regex)

```
PS C:\htb> Get-ChildItem -Recurse -Path N:\ | Select-String "cred" -List
```


## Linux

#### Linux - Mount

```
sudo mkdir /mnt/Finance
sudo mount -t cifs -o username=plaintext, password=Password123, domain=.//192.168.220.129/Finance /mnt/Finance
```

**As an alternative, we can use a credential file :** 

```
mount -t cifs //192.168.220.129/Finance /mnt/Finance -o credentials=/path/credentialfile
```

The file `credentialfile` has to be structured like this:

#### CredentialFile

```
`username=plaintext password=Password123 domain=.`
```

> Note: We need to install `cifs-utils` to connect to an SMB share folder. To install it we can execute from the command line `sudo apt install cifs-utils`.

#### Linux - Find


```
`find /mnt/Finance/ -name *cred* mnt/Finance/Contracts/private/credentials.txt`
```

Next, let's find files that contain the string `cred`:

```
grep -rn /mnt/Finance/ -ie cred /mnt/Finance/Contracts/private/credentials.txt:1:admin:SecureCredentials! /mnt/Finance/Contracts/private/secret.txt:1:file with all
```


#### Linux - Install Evolution


```
sudo apt-get install evolution 
```


>Note: If an error appears when starting evolution indicating "bwrap: Can't create file at ...", use this command to start evolution `export WEBKIT_FORCE_SANDBOX=0 && evolution`.

[video demo](https://www.youtube.com/watch?v=xelO2CiaSVs)

#### Databases

## Command Line Utilities to Interact with Databases

#### Linux - SQSH

```bash
sqsh -S 10.129.20.13 -U username -P Password123
```

#### Windows - SQLCMD
```
sqlcmd -S 10.129.20.13 -U username -P Password123
```

#### Linux - MySQL
```
mysql -u username -pPassword123 -h 10.129.20.13
```

#### Windows - MySQL

```
mysql.exe -u username -pPassword123 -h 10.129.20.13
```

#### GUI Application
- **dbeaver :** Multi platform database tool which supports windows, mac and linux and supports multiple database engines such as MSSQL, MySQ:, PostgreSQL ect.
- dbeaver : [link](https://github.com/dbeaver/dbeaver/releases)
- vido demo : [link](https://www.youtube.com/watch?v=gU6iQP5rFMw)
```
sudo dpkg -i dbeaver-<version>.deb
dbeaver &
```

## Tools to Interact with Common Services

| SMB | FTP | Email | Databases |
|-----|-----|-------|-----------|
| smbclient | ftp | Thunderbird | mssql-cli |
| CrackMapExec | lftp | Claws | mycli |
| SMBMap | ncftp | Geary | mssqlclient.py |
| Impacket | filezilla | MailSpring | dbeaver |
| psexec.py | crossftp | mutt | MySQL Workbench |
| smbexec.py | | mailutils | SQL Server Management Studio (SSMS) |
| | | sendEmail | |
| | | swaks | |
| | | sendmail | |

